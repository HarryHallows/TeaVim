-- Your personal overrides. This file is never touched by TeaVim updates.
-- Return a table with any keys from teavim/config.lua you want to change.
-- Uncomment and edit what you need.

return {
  profile = "vscode_pure",       -- "vim" | "vscode" | "vscode_pure"
  -- leader  = " ",
  -- onboarding = true,        -- false to suppress, "force" to always show

  -- features = {
  --   explorer = true,
  --   fuzzy    = true,
  --   terminal = true,
  --   lsp      = true,
  -- },
}
